## 1. Source-Identity Access

- [x] 1.1 Add the allocation-free `HbkPropertyView::source_id()` delegation and focused member/global plus owned/X1 contract assertions.

## 2. Completion

- [x] 2.1 Restore the downstream accepted real-X1 semantic digest, run affected formatting/strict Clippy/tests and strict OpenSpec validation, record the actual-diff codebase-design review, bump HBK to `0.2.7`, and leave the change committed and ready to archive.
