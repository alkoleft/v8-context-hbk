## 1. Equal-input typed-table experiment

- [x] 1.1 Implement the private no-`StringId` candidate: typed primary-name
  tables whose row ordinals are IDs, owner-completed callable/property IDs,
  completed-ID owner-membership vectors, separate aliases, in-lookup
  normalization, one-search routing, temporary duplicate dropping/counters and
  differential tests including absent-under-owner behavior. Run focused/full
  feature verification and the isolated frozen release benchmark; record
  same-run results in `measurements.md`.

## 2. Completion gates

- [x] 2.1 Reconcile the actual diff with the codebase-design record,
  `Structure impact` and `Reintroduction guard`; obtain fresh independent
  review, bump the workspace patch version, run strict workspace/OpenSpec
  validation, archive/synchronize the change, inspect staged scope and create a
  task-scoped Conventional Commit.
