## 1. Borrowed Semantic Role Vertical Slice

- [x] 1.1 Add the neutral dependency only to `syntax-helper-search`; deepen the
  existing H0/X1-neutral view internals enough to resolve borrowed names;
  implement direct callable/signature/parameter/type-declaration roles and the
  single filtered borrowed property role for type-member and BSL-global
  properties; add owned/X1 parity, arity and
  structural guards; reconcile architecture, versioning, review and strict
  verification; then archive and commit the completed change.
