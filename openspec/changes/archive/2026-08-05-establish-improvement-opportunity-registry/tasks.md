## 1. Establish the opportunity registry

- [x] 1.1 Add the non-authoritative improvement-opportunity registry with the ID-only hash-index and `string-interner` records; verify its no-backlog structure and evidence links, record the actual-diff codebase-design pass, apply the patch version bump, pass strict active and canonical OpenSpec validation, archive with capability synchronization, inspect staged scope, and create the task-scoped Conventional Commit.
