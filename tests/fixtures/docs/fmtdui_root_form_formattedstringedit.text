Constructor of strings in different languages If it is supposed to use multiple interface languages in a configuration (if more than one object is defined on the "Languages" sub-branch, on the "Common" branch of the configuration tree), you can type the decoration text using different languages. For each "Languages" object, the dialog box contains fields for typing texts. In the upper part of the constructor, you can choose the style of a string by using radio buttons.
